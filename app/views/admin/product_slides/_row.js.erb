{
  'DT_RowId':   '<%= product_slide.id %>',
  'image': '<%= image_tag product_slide.image.url(:thumb) if product_slide.image.present? %>',
  'order': '<%= product_slide.order %>',
  'actions': '<%= link_to("Edit", edit_admin_product_slide_path(product_slide), remote: true, class: "btn btn-info btn-xs").html_safe %> '
  + '<%= link_to("Delete", admin_product_slide_path(product_slide), remote: true, method: :delete, data: { confirm: 'Are you sure to delete this image?' }, class: "btn btn-danger btn-xs").html_safe %> '
}
