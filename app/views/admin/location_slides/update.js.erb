<% if @location_slide.errors.any? %>
  toastr.error("<%= @location_slide.errors.full_messages.join('<br>') %>");
<% else %>
  call_ajax_by_url('<%= admin_location_location_slides_path(@location_slide.location) %>');
<% end %>
