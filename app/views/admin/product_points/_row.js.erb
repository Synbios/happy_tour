{
  'DT_RowId':   '<%= product_point.id %>',
  'text': '<%= product_point.text %>',
  'order': '<%= product_point.order %>',
  'actions': '<%= link_to("Edit", edit_admin_product_point_path(product_point), remote: true, class: "btn btn-info btn-xs").html_safe %> '
  + '<%= link_to("Delete", admin_product_point_path(product_point), remote: true, method: :delete, data: { confirm: 'Are you sure to delete this image?' }, class: "btn btn-danger btn-xs").html_safe %> '
}
