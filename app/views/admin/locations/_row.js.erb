{
  'DT_RowId':   '<%= location.id %>',
  'chinese': '<%= location.chinese %>',
  'english': '<%= location.english %>',
  'created_at': '<%= location.created_at.strftime("%Y年%m月%d日") %>',
  'actions': '<%= link_to("Edit", edit_admin_location_path(location), remote: true, class: "btn btn-info btn-xs").html_safe %> '
  + '<%= link_to("Delete", admin_location_path(location), remote: true, method: :delete, data: { confirm: 'Are you sure to delete this location?' }, class: "btn btn-danger btn-xs").html_safe %> '
}
