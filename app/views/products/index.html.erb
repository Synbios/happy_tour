<% content_for :header_class do %>lightHeader<% end %>

<section class="pageTitle" style="background-image:url(img/pages/page-title-bg4.jpg);">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <div class="titleTable">
          <div class="titleTableInner">
            <div class="pageTitleInfo">
              <h1>所有热门线路</h1>
              <div class="under-border"></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="mainContentSection">
  <div class="container">
    <div class="row">
      <% @products.each do |product| %>
        <%= render partial: "products/card.html.erb", locals: {product: product} %>
      <% end %>
    </div>
  </div>
</section>
