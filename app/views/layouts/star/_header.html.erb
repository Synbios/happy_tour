<header>
  <nav class="navbar navbar-default navbar-main navbar-fixed-top <%= yield(:header_class) %>" role="navigation">
    <div class="container">

      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "", root_path, class: "navbar-brand" %>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse navbar-ex1-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li class="active">
            <%= link_to "首页", root_path %>
          </li>
          <li class="">
            <%= link_to "关于我们", contact_path %>
          </li>
          <li class="dropdown singleDrop ">
            <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">经典线路</a>
            <ul class="dropdown-menu dropdown-menu-right">
              <li class="dropdown dropdown-submenu">
                <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown">温哥华周边 <i class="fa fa-chevron-right" aria-hidden="true"></i></a>
                <ul class="dropdown-menu">
                  <% Product.where(category: "经典线路-温哥华周边").each do |product| %>
                  <li>
                    <%= link_to product.name, product_path(product) %>
                  </li>
                  <% end %>
                </ul>
              </li>
              <li class="dropdown dropdown-submenu">
                <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown">落基山脉 <i class="fa fa-chevron-right" aria-hidden="true"></i></a>
                <ul class="dropdown-menu">
                  <% Product.where(category: "经典线路-落基山脉").each do |product| %>
                  <li>
                    <%= link_to product.name, product_path(product) %>
                  </li>
                  <% end %>
                </ul>
              </li>
              <li class="dropdown dropdown-submenu">
                <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown">加东 <i class="fa fa-chevron-right" aria-hidden="true"></i></a>
                <ul class="dropdown-menu">
                  <% Product.where(category: "经典线路-加东").each do |product| %>
                  <li>
                    <%= link_to product.name, product_path(product) %>
                  </li>
                  <% end %>
                </ul>
              </li>
              <li class="dropdown dropdown-submenu">
                <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown">美国 <i class="fa fa-chevron-right" aria-hidden="true"></i></a>
                <ul class="dropdown-menu">
                  <% Product.where(category: "经典线路-美国").each do |product| %>
                  <li>
                    <%= link_to product.name, product_path(product) %>
                  </li>
                  <% end %>
                </ul>
              </li>
            </ul>
          </li>
          <li class="dropdown megaDropMenu ">
            <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown" data-hover="dropdown" data-delay="300" data-close-others="true" aria-expanded="false">特色旅游</a>
            <ul class="row dropdown-menu">
              <li class="col-sm-6 col-xs-12">
                <ul class="list-unstyled">
                  <li>加拿大</li>
                  <% Product.where(category: "特色旅游-加拿大").each do |product| %>
                  <li>
                    <%= link_to product.name, product_path(product) %>
                  </li>
                  <% end %>
                </ul>
              </li>
              <li class="col-sm-6 col-xs-12">
                <ul class="list-unstyled">
                  <li>环游世界</li>
                  <% Product.where(category: "特色旅游-环游世界").each do |product| %>
                  <li>
                    <%= link_to product.name, product_path(product) %>
                  </li>
                  <% end %>
                </ul>
              </li>
            </ul>
          </li>
          <li class="dropdown singleDrop">
            <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">海岛度假</a>
            <ul class="dropdown-menu dropdown-menu-right">
              <% Product.where(category: "海岛度假").each do |product| %>
              <li>
                <%= link_to product.name, product_path(product) %>
              </li>
              <% end %>
            </ul>
          </li>
          <li class="">
            <%= link_to "旅游定制", customize_path %>
          </li>
          <li class="">
            <%= link_to "机票酒店", airfares_and_hotels_path %>
          </li>
        </ul>
      </div>

    </div>
  </nav>
</header>
